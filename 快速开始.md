# 🚀 快速开始指南

## ✅ 当前状态

- ✅ 项目文件已创建
- ✅ Git 仓库已初始化
- ✅ 代码已提交

## 📝 接下来您需要做的 3 步

### 第 1 步：上传到 GitHub（5 分钟）

1. 访问 https://github.com 并登录
2. 点击右上角 "+" → "New repository"
3. 仓库名：`shopify-mcp-server`
4. 选择 Public，点击 "Create repository"
5. 复制仓库地址（类似：`https://github.com/您的用户名/shopify-mcp-server.git`）

然后在 PowerShell 中执行（在 `shopify-mcp-server` 目录下）：
```powershell
git remote add origin https://github.com/您的用户名/shopify-mcp-server.git
git branch -M main
git push -u origin main
```

**提示**：如果要求输入密码，请使用 GitHub Personal Access Token（不是密码）

### 第 2 步：部署到 Vercel（5 分钟）

1. 访问 https://vercel.com 并用 GitHub 登录
2. 点击 "Add New..." → "Project"
3. 选择 `shopify-mcp-server` 仓库，点击 "Import"
4. **重要**：在 "Environment Variables" 添加：
   - `SHOPIFY_STORE` = `happier-shopping-3.myshopify.com`
   - `SHOPIFY_ACCESS_TOKEN` = `your_shopify_access_token_here`
5. 点击 "Deploy"
6. 等待部署完成，复制项目 URL（类似：`https://xxx.vercel.app`）

### 第 3 步：在 Dify 中配置（2 分钟）

1. 登录 Dify（social3085@social.helwan.edu.eg / Welcome@123）
2. 工具 → MCP → 添加 MCP 服务器（HTTP）
3. 填写：
   - 服务器 URL：`https://您的vercel地址.vercel.app`
   - 名称：`Shopify MCP`
   - 服务器标识符：`shopify_mcp`
4. 保存

## 📚 详细步骤

查看 `Vercel部署步骤-详细版.md` 获取完整的手把手教程。

## 🎯 验证部署

访问：`https://您的vercel地址.vercel.app/health`

应该看到：
```json
{
  "status": "ok",
  "service": "shopify-mcp-server",
  "store": "happier-shopping-3.myshopify.com",
  "token_configured": true
}
```

## ❓ 需要帮助？

查看 `Vercel部署步骤-详细版.md` 中的"遇到问题"部分。

